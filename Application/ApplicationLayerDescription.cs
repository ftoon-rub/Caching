using Core;

namespace Application
{
    public class ApplicationLayerDescription : ILayerDescription
    {
        public void LayerDescription()
        {
            //Application Layer:
            //Use Cases(or Services): This layer handles application-specific business logic, typically implemented as use cases or application services.
            //DTOs(Data Transfer Objects): Models used to transfer data between layers, often used in requests and responses.
            //Interfaces: Interfaces related to the application layer, often including use case abstractions
            throw new NotImplementedException();
        }
    }
}
